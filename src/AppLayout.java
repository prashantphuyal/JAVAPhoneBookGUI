import javax.swing.*;

public interface AppLayout {
    /*
     * Interface for app layout is created so that se don't have to name for each JPanel in each java Panels
     * One name i.e panelUI get work done in each panels */
    JPanel panelUI();

}
